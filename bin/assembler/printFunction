print "Hello World" #This is the string to be printed
Print: #you can only  do comments on the same line as code, not its own line
load $R1 $Rstring #load character
load $R2 $Rscreen #load screen position
store $R1 $R2 #store on screen
load $R3 0x0001 #load 1 into $R3
load $R4 0x0060 #'`' marks the end of the string
add $Rstring $R3
test $Rstring $R4
add $Rscreen $R3
jmpNE Print:
